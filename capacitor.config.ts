import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'personal_workout_planner',
  webDir: 'www'
};

export default config;
